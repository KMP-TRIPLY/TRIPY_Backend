# application-local.yml 작성 예시
# 이 파일을 복사해서 application-local.yml 을 만들고 실제 값을 채워주세요
# application-local.yml 은 .gitignore 에 추가되어 있어 커밋되지 않습니다

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/triply
    username: postgres
    password: your_db_password

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: your_kakao_client_id
            client-secret: your_kakao_client_secret

          naver:
            client-id: your_naver_client_id
            client-secret: your_naver_client_secret

          google:
            client-id: your_google_client_id
            client-secret: your_google_client_secret

jwt:
  secret: your_jwt_secret_base64_encoded

tourism:
  api:
    key: your_tourism_api_key
    kor-service-base-url: https://apis.data.go.kr/B551011/KorService2

# 백제 역사 코스(스토리 A/B/C) 시드 데이터를 최초 1회 등록하려면 true로 설정 후 서버를 한 번 기동하세요.
# 이미 등록된 코스가 있으면 자동으로 건너뜁니다.
app:
  seed:
    enabled: true

# 미션 사진 AI 인증(OpenAI Vision) 키는 이 파일이 아니라 프로젝트 루트 .env 에 둔다.
# 운영 서버와 같은 변수 이름을 쓰고, 프로필과 무관하게 읽힌다.
#   OPENAI_API_KEY=sk-...
# 키가 없으면 사진 미션 제출이 503 으로 막힌다. 판정 없이 굴리려면 MISSION_PHOTO_AI_ENABLED=false 로 명시한다.
